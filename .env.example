# Server
PORT=3001
NODE_ENV=production

# Auth — CHANGE THIS! Generate with: openssl rand -hex 32
JWT_SECRET=change-me-in-production

# Database
DATABASE_PATH=./data/faq.db

# AI Features (optional) — Get key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# CORS (optional) — Comma-separated allowed origins
# ALLOWED_ORIGINS=https://yourdomain.com
